/* LibLab - For Library development.
  This is the main .cpp file that you should compile and run when testing on PC, but main() is in PCOnly.h. Add your code there, not here.
  You can either use my Build.sh script (which is Linux only...), or set up make or something similar of your choice...
*/

#define PCSoftware
#include "../LibLib.h"
